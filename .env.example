# Copy this file to .env and fill in your real key. Never commit .env itself.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional. Set in production to restrict CORS to your deployed frontend origin
# (e.g. https://stadiumpilot.onrender.com). Leave unset for local dev / same-origin deploys.
FRONTEND_URL=
